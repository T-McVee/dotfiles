# Dotfiles

Public repository — never commit secrets, credentials, or private configuration.

Shared across multiple machines to provide a consistent environment.

Do not add a Co-Authored-By line to commit messages.
